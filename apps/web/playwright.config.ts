// AnimBook Playwright config moved to `apps/web/playwright.config.mjs` (ESM).
// The `.mjs` sibling is invisible to the Next.js typecheck + webpack
// pipeline — install with `npm i -D @playwright/test` and run
// `npx playwright test` from `apps/web/`.
