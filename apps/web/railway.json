{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev --production=false --legacy-peer-deps --ignore-scripts --workspace=apps/web --include-workspace-root && cd apps/web && npx next build"
  },
  "deploy": {
    "startCommand": "cd apps/web && npx next start -p 3000",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
